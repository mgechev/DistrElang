def add(a, b)
  return a + b;
enddef;

def add2(a)
  return add(a, 2);
enddef;

def multi(a, b)
  return a * b;
enddef;

def complex(a, b)
  return multi(add2(a), b);
enddef;


print '(19 + 2) * 2 = ';
print complex(19, 2);