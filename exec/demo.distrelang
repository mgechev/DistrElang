def add(a, b)
  return a + b;
enddef;

def add2(a)
  return add(a, 2);
enddef;

def multi(a, b)
  return a * b;
enddef;

def complex(a, b)
  return multi(add2(a), b);
enddef;

print 'Add 1 and 2: ';
print add(1, 2);
print ', ';
print 'add 2 to 40: ';
print add2(40);
print ', ';
print '(2 + 2) * 3: ';
print complex(2, 3);
print '.';